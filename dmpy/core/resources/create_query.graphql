mutation createQuery($query: QueryObjInput!) {
        createQuery(query: $query) {
            requester
            id
            queryString
            studyId
            projectId
            status
            error
            cancelled
            data_requested
            cohort
            new_fields
        }
    }
